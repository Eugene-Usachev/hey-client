import React, {memo, FC} from 'react';
import {RegistrationBackground} from "./registration/RegistrationBackground/RegistrationBackground";

export const Registration: FC = memo(() => {

	return (
		<RegistrationBackground />
	);
});