import React, {memo, FC} from 'react';
import styles from './TemplateName.module.scss';

export const TemplateName: FC = memo(() => {

	return (
		<div className={styles.templateName}>

		</div>
	);
});