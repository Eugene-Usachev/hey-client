import React, {FC} from 'react';
import {observer} from "mobx-react-lite";
import styles from './TemplateName.module.scss';

export const TemplateName: FC = observer(() => {

    return (
        <div className={styles.templateName}>

        </div>
    );
});